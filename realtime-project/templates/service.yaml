apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime-project.fullname" . }}
  labels:
    {{- include "realtime-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "realtime-project.selectorLabels" . | nindent 4 }}
