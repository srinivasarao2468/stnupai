apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "realtime-project.fullname" . }}-test-connection"
  labels:
    {{- include "realtime-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "realtime-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
